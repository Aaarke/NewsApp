package com.example.newsapp.utility

interface Constant {
    interface DateFormat {
        companion object {
            const val END_USER_DATE_FORMAT3 = "dd MMM yyyy"
            const val STANDARD_DATE_FORMAT_TZ = "yyyy-MM-dd'T'HH:mm:ss'Z'"
        }
    }
}